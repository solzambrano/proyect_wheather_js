El proyecto es de Frontend Mentor, inicalmente esta planteado para utilizar la API de open.meteo (https://open-meteo.com/).

A mitad del desarrollo la API se cayo, por lo que use la API de weather (https://goweather.xyz/weather/Argentina).

Al cambiar de API , quedaron cosas sin funcionalidad como la cantidad de dias que se presentan y el poder cambiar las unidades, hubieron datos que se tuvieron que mockear para que no rompa con el diseño.

La pagina usa html, css y js
